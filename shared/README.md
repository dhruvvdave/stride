# Shared Code

This directory contains code shared between the mobile and web applications.

## Structure

- `types/` - TypeScript type definitions (future)
- `constants/` - Shared constants
- `utils/` - Shared utility functions
